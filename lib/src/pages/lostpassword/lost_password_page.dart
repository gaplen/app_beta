import 'package:flutter/material.dart';


class LostPasswordPage extends StatelessWidget {
  const LostPasswordPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('lostpassword'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('puto perro'),
      ),
    );
  }
}